// General Imports
import React from "react";

const Header = (props) => {
    return(
        <header>
            <h2>Project 7: Gallery App in React</h2>
        </header>
    )
}

export default Header;